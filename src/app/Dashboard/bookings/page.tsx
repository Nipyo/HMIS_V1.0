import BookingsTable from "@/components/Bookings/BookingsTable";
import React from "react";

const page = ( { }) => {
  return (
    <div>
      <BookingsTable />
    </div>
  );
};

export default page;
